# Lane LOAD — status

- **Phase:** DONE (green, committed). Ready for the integrator to merge LAST.
- **Branch:** `m5-load` · **DB:** `specula_wt_load` (seeded: demo-user, 13 postings).
- **Blocked?** No. **Waiting on?** Nothing. Some E2E assertions are intentionally
  skip-guarded on peer lanes not yet on this branch (see below) — by design, per the brief.
- **Shared surfaces touched:** none. All work is new files under `load/` and
  `apps/web/e2e/authed/`. No shared-types/migration/config/registry edits.

## Delivered

- `load/script.js` + `load/README.md` — k6 harness. Verified against the local stack:
  `browse` (20 VUs, 70s) → **0% errors**, `GET /jobs` p95 **251ms**, `GET /dashboard` p95
  **13ms**; `ingest` smoke (`INGEST=1`) → `POST /runs` 201, p95 **54ms**. Baseline recorded
  in `load/README.md`.
- `apps/web/e2e/authed/{dashboard,export,ratelimit}.spec.ts` — new Playwright specs, inside
  the existing `authed` project glob (no `playwright.config.ts` edit). Lint + typecheck +
  prettier clean; all three collected by `playwright test --list`.

## Pending peer-lane merges (E2E specs skip-guarded, NOT asserting against stubs)

LOAD merges LAST and rebases onto the integrated `main`. These features are NOT on this
branch yet, so each spec is guarded by a single `const PENDING = true` + a `beforeEach`
`test.skip(PENDING, …)`. After rebase: flip `PENDING → false` and tighten selectors against
the merged DOM.

- `dashboard.spec.ts` → **DASH** (`/dashboard` view + `DashboardSummary`). Now:
  `GET /api/v1/dashboard` = `{status:not_implemented}` stub; no `(app)/dashboard` page.
- `export.spec.ts` → **DATA** (`/settings` export → download `ExportBundle`). Now:
  `GET /api/v1/account` stub; no `(app)/settings` page.
- `ratelimit.spec.ts` → **NET** (run-trigger 429 surfaced in the UI). Now: cap config exists
  (`run_rate_limit_per_hour`/`run_cooldown_s`) but no enforcement merged.

## Coverage flagged (not silently capped)

- `GET /dashboard` load exercises the pre-merge stub (a real 200); re-baseline the
  `DashboardSummary` aggregation cost after DASH merges.
- No `429`s observed under `INGEST=1` (`rate_limited_429 = 0`) — NET cap not on this branch.

## Notes for the integrator

- No `apps/api/` files added: the optional cross-lane API tests would target unmerged
  endpoints (dashboard summary, export), so they belong post-rebase, not against stubs.
- Final DOM selectors for the DASH/DATA views are unknown until rebase; specs use
  contract-grounded role/text assertions that will be tightened then.
