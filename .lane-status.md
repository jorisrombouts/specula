# Lane NET — status

**Phase:** DONE — green, committed AND pushed to `origin/m5-net`. See `.lane-report.md`.
**Blocked?** no
**Waiting on?** nothing (ready for integrator; NET merges first)

## Delivered
- Rate-limit gate: `ratelimit.py` + `Depends(rate_limit_guard)` on `POST /runs` +
  `enforce_rate_limit` on the approve→ingest trigger. 429 body matches frozen `RateLimitError`.
- Politeness (`pipeline/http.py`) + provenance (`pipeline/fetch.py`): already delivered in M3,
  verified green, unchanged (no rewrite).

## Green gate
`uv run pytest -q` → 409 passed, 1 deselected · `mypy .` clean · `ruff check` clean ·
`ruff format --check` clean.

## Shared surfaces the integrator must reconcile
- **No `main.py` edit** — the frozen `RateLimitError` bare body is rendered by a per-router
  `RateLimitedRoute` (`route_class=`) instead of an app-level handler, because OBS owns `main.py`
  exclusively. No conflict with OBS.
- **One test amendment outside my owned set:**
  `tests/test_approvals_api.py::test_approve_is_idempotent_on_duplicate_domain` now sets
  `run_cooldown_s=0` (the gate would 429 its 2nd rapid same-user approve). Details in report.

## Open questions
- none
