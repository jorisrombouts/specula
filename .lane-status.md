# Lane DATA — status

**Phase:** COMPLETE — lane gates green (API + web lint/typecheck/build/test/e2e); committed + pushed on `m5-data`, draft PR #16
**Blocked?** no
**Waiting on?** integrator — the shared Playwright **visual-regression** CI step is red on
`main` too (pre-existing `bffFetch: no authenticated session` harness issue, unrelated to DATA).
Every step this lane touches passes in branch CI. See `.lane-report.md` → "Branch CI".

## Plan
- [x] `schemas/account.py` — ExportBundle + row models (CamelModel, cost→float, vectors excluded)
- [x] `services/account.py` — `export_account` (RLS-scoped reads, excl. skills_taxonomy) + `delete_account` (DELETE users → FK cascade)
- [x] `routers/account.py` — `GET /account/export`, `DELETE /account`
- [x] `routers/company.py` — `POST /companies/{id}/opt-out` (toggle opt_out=true, user-scoped)
- [x] backend tests: export shape+tenancy, delete cascade (every per-user table, B intact, taxonomy intact), opt-out
- [x] backend gate GREEN: `pytest` (405 pass) · `mypy` · `ruff check` · `ruff format --check`
- [x] web slice: settings page + components + BFF routes + lib/api/account.ts
- [x] web tests (lib/api/account.test.ts + settings-view.test.tsx)
- [x] web gate GREEN: `lint` · `typecheck` · `test` (175 pass) · `format:check` · `build`

## Shared surfaces I had to touch
- **PENDING AMENDMENT (services/run.py — OBS-owned, NOT edited by this lane):** opt-out
  enforcement needs a one-line guard in `ingest_company`: after loading the company, add
  `or company.opt_out` to the early-return guard so an opted-out company is skipped on the
  next ingest. Flagged for the integrator (serialized amendment, applied during DATA
  integration which follows OBS). The toggle endpoint + column write are fully in-lane.

## Open questions
- none yet
